{
  "private": true,
  "scripts": {
    "favicon-generate": "real-favicon generate faviconDescription.json build/faviconData.json dist",
    "favicon-inject": "real-favicon inject build/faviconData.json build src/wrapper.html",
    "favicon-check": "real-favicon check-for-update --fail-on-update build/faviconData.json",
    "html-minifier": "html-minifier build/bundled.html -o dist/index.html --collapse-whitespace --remove-comments --use-short-doctype --minify-css true",
    "marked": "marked -o build/content.html -i src/index.md --mangle",
    "qrcode": "qrcode -o dist/qr.svg https://loic.vour.ch",
    "sass": "node-sass --no-source-map src/style.scss dist/style.css --output-style compressed",
    "test": "npm run favicon-check"
  },
  "dependencies": {
    "cli-real-favicon": "0.0.8",
    "html-minifier": "^4.0.0",
    "marked": "^1.2.2",
    "node-sass": "^5.0.0",
    "qrcode": "^1.4.4"
  }
}
